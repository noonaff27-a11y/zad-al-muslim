const id=Number(new URLSearchParams(location.search).get('id'))||1;let stories=[];let s,idx=0;
fetch('stories.json').then(r=>r.json()).then(d=>{stories=d;s=stories.find(x=>x.id===id)||stories[0];document.getElementById('storyTitle').textContent=s.title;render();});
function render(){const p=s.pages[idx];document.getElementById('page').innerHTML=`<article class="page-card"><img src="${p.image}" alt="رسمة تعبر عن: ${p.title}"><div class="page-text"><span class="tag">الصفحة ${idx+1} من ${s.pages.length}</span><h1>${p.title}</h1><p>${p.text}</p></div></article>`;document.getElementById('pageNo').textContent=`${idx+1} / ${s.pages.length}`;document.getElementById('progress').style.width=((idx+1)/s.pages.length*100)+'%';document.getElementById('prev').disabled=idx===0;document.getElementById('next').disabled=idx===s.pages.length-1;}
document.getElementById('prev').onclick=()=>{if(idx>0){idx--;render();scrollTo(0,0)}};document.getElementById('next').onclick=()=>{if(idx<s.pages.length-1){idx++;render();scrollTo(0,0)}};